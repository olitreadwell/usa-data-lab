# Loop notes

One dated line per grow-loop iteration: what shipped, or what blocked it.
- 2026-09-24: shipped the second microsite, `hawaii-quakes` (commit 0d11ba2, feat(site)). The loop wrote the USGS adapter, the band transform, the chart, the fixtures, and 17 tests, then died at 00:01:34 on `429 Too Many Requests` after 206,768 tokens and left 16 files uncommitted, so the 06:50 run skipped on the dirty-tree rule (`consecutiveSkips: 1`). A supervised session finished the wiring by hand: a `microsites.ts` entry, a line in `PUBLISHED_MICROSITES`, a `[category]/[slug]` branch, and `docs/experiments/hawaii-quakes/`. The page renders the 2025 window and the numbers agree with a fresh call to the FDSN event query today: 264 earthquakes at magnitude 2.5 or higher, bands 176/65/18/5, strongest M4.41 on 15 March 2025 (53 km west of Hawaiian Ocean View), deepest 59.8 km on 22 February 2025 (13 km west of Puako). Gates run before the commit: `format:check`, `lint` (0 errors), `type-check`, `test:coverage` (143 web tests, 35 adapter tests), `build` (prerenders `/environment/hawaii-quakes`), and `test:a11y` with `E2E_PORT=3100` (28 passed). Source host note: `earthquake.usgs.gov` answers 200 to scripted requests; `www.usgs.gov` answers 403, the same way `www.bls.gov` does, so the three references point at the catalogue host.
- 2026-09-25: shipped the third microsite, `cdc-county-obesity` (commit 58b8cd1, feat(stories)). No vendored adapter was free, so the loop added one upstream first: `cdc-county-obesity` in `packages/usa-sources` of usa-open-data-connectors, on `feat/connectors/add_cdc_county_obesity` (PR #29, that repo's `npm run check` green), then vendored it here with `scripts/sync-connectors.mjs --from ../usa-open-data-connectors`. The page reads 2,956 counties with an estimate: 16.7 percent in Boulder County, Colorado up to 52.9 percent in Perry County, Alabama, median county 37.9 percent against the release's own national row of 32.8 percent, weighted by population 33.28 percent, 2,502 counties above the national figure, and no obesity rows at all for Kentucky or Pennsylvania in this release. The same commit fixed a stale number on the jobless-rate story: its window was pinned to 2006-2025, so the site showed December 2025 as the latest month while the BLS series already carried August 2026 at 4.1 percent. The window now rolls with the calendar, the committed snapshot is trimmed to the same window and refreshed through August 2026, and the built page renders `Rate in Aug 2026 4.1%`. Gates before the commit: `npm run check` with `E2E_PORT=3200` (format, lint, type-check, 167 web unit tests, build, smoke, 48 e2e including the a11y pass on the new route, internal links). Source host notes: `data.cdc.gov` answers 200 to scripted requests; `www.cdc.gov` answers 403 the way `www.bls.gov` does, so the PLACES program page was opened in a browser before linking it.
- 2026-09-26: shipped the fourth microsite, `us-temperature-record` (commit fe44af7, feat(stories)). No vendored adapter was free, so the loop added one upstream first: `ncei-annual-temperature` in `packages/usa-sources` of usa-open-data-connectors, on `feat/connectors/add_ncei_annual_temperature` (PR #32, that repo's `npm run check` green at 99.68 percent statements, live smoke green, all PR checks passing), then vendored it here with `scripts/sync-connectors.mjs --from ../usa-open-data-connectors`. The page reads the NOAA NCEI Climate at a Glance calendar-year series for the contiguous United States: 131 years from 1895 to 2025, warmest 2024 at 55.48 °F, coldest 1917 at 50.05 °F, a 20th century average of 52.01 °F from the same file, 2025 at 54.62 °F and 2.61 °F above it, and all 26 years from 2000 to 2025 above that average. A fresh download on 2026-09-26 returns the same figures, and the deployed page renders 55.48 °F, +2.61 °F, and 26 of 26. Two things worth recording. The chart is a strip chart of one dot per year in decade rows, and the first attempt gave recharts a category y axis, which left 117 of the 131 dots with no y position and floated them at the top of the SVG; the chart now maps each year to a numeric row index. And the new data source label is long enough to widen the home page filter selects past the visual viewport on a 375 px screen, which pushed the fixed report button out of reach and failed the mobile report-dialog e2e test, so the filter labels now shrink to their column (`min-w-0` label, `w-full` select). The audit at the start of the run found no broken stories: `npm run check` with `E2E_PORT=3200` was green, and the three live stories matched their sources that day (BLS 4.1 percent for August 2026, 264 Hawaii earthquakes in 2025, CDC obesity 16.7 to 52.9 percent across 2,956 counties). Gates before the commit: `npm run check` (format, lint 0 errors, type-check, 189 web unit tests, 69 adapter tests, build, smoke, 52 e2e including the a11y pass on `/energy/us-temperature-record`, internal links), then the Vercel production deployment for fe44af7 reached success. Source host notes: `www.ncei.noaa.gov` answers 200 to scripted requests and serves the whole 131-year record as one 20 KB CSV; a calendar year joins the file only after December, so the newest row is always a complete year.
- 2026-09-27: shipped the fifth microsite, `battery-sea-level` (commit 919c4d0, feat(stories)). No vendored adapter was free, so the loop added one upstream first: `noaa-sea-level` in `packages/usa-sources` of usa-open-data-connectors, on `feat/connectors/add_noaa_sea_level` (PR #33, that repo's `npm run check` green at 99.75 percent statements and 100 percent lines on the new file), then vendored it here with `scripts/sync-connectors.mjs --from ../usa-open-data-connectors`. The page reads NOAA CO-OPS monthly mean sea level for station 8518750, The Battery in New York: 155 calendar years from 1856 to 2025 with fifteen years carrying no monthly value, an annual mean of 0.36 m below the 1983 to 2001 tidal datum in 1856 against 0.12 m above it in 2025, a rise of 0.48 m (19 inches), a fitted trend of 2.95 mm a year, the ten oldest years averaging 0.354 m below the datum against 0.131 m above for the ten newest, and all ten of the highest years inside 2010 to 2025. The audit at the start of the run found no broken stories: the four live pages matched their sources that day (BLS 4.1 percent for August 2026, 264 Hawaii earthquakes in 2025, CDC obesity 16.7 to 52.9 percent across 2,956 counties, NOAA 2024 at 55.48 °F and 2025 at 54.62 °F), and `npm run check` with `E2E_PORT=3200` was green. One false start on the way: the first `npm run check` used the default port 3000 and all 52 e2e tests died on `ERR_CONNECTION_REFUSED` from the `serve out` web server, and the same suite passed on 3200. Gates before the commit: `npm run check` (format, lint 0 errors, type-check, 208 web unit tests, 93 adapter tests, build prerendering `/environment/battery-sea-level`, smoke, 56 e2e including the a11y pass on the new route, internal links), then the Vercel production deployment for 919c4d0 reached Ready in 33 seconds and the live page renders +0.48 m, +0.12 m, and 2.95 mm a year. Three notes for the next run. PR #33 is stacked on PR #32, which is still open, so the branch carries the temperature commit until #32 lands and this repo's vendored copy keeps both adapters. The connectors repo could not commit at all before this run: the pre-commit hook runs `npm run typecheck`, which was `tsc --noEmit` at a repo root that has no `tsconfig.json`, so tsc printed its help and exited 1 on every commit, and the branch points that script at the workspace type-check the CI job already runs. And every story page prints the heading "How to read this chart" twice, once from the microsite config and once from the chart's own explain block; that is pre-existing on all four older pages and was left alone. Source host notes: `api.tidesandcurrents.noaa.gov` answers 200 to scripted requests and returns the whole 170-year monthly record in one 458 KB response; `tidesandcurrents.noaa.gov` answers 200 for the station, sea level trends, and tidal datums pages, so all three are linked directly; a CO-OPS level error arrives in the body with HTTP 200 as an `error.message` object, which the parser reports as an API error, while a wrong product name answers with plain text.
